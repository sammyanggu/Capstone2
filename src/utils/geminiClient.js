// Placeholder for AI code review
// TODO: Integrate with cloud-based AI service (e.g., Gemini API, OpenAI, etc.)

export async function getCodeReview(code, language, task) {
  if (!code || code.trim().length === 0) {
    return [{ level: "info", message: "No code yet — start typing to get AI feedback." }];
  }

  // Placeholder feedback
  return [
    { level: "info", message: "AI code review service coming soon! Configure an API key to enable." }
  ];
}
